#include <stdio.h>

#define SWAP(int_1, int_2) { int temp = int_1; int_1 = int_2; int_2 = temp; }

int x, y;

int main()
{
  x = 10;
  y = 20;
  
  SWAP(x, y)

  printf("x = %d, y = %d\n", x, y);

  return 0;
}
